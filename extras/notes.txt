- https://github.com/Destrigo/42_New_Curriculum/tree/main/milestone_3/call_me_maybe
- https://github.com/sousampere/42_call_me_maybe_v1.2/tree/main
- https://github.com/KeroBeros68/Call-Me-Maybe

Todos:
x Protect when model did not load properly, i.e. when input a invalid model name

- When there are less numbers in the prompt than number parameters, still use the numbers in prompt to guarantee at least these numbers are correct?
-> only works when the order does not matter.
e.g. "What sixty-seven plus -23454674?" -> a: 67.0, b: 2345467676767
Seems to work ok with simple numbers:
"What is seventy-five plus 49?" -> a: 75.0, b: "49.0"
Maybe leave it for now.

- Somehow newly added functions are not matched:
[
    {
        "prompt": "What is -49654658.45 divided by 35453?",
        "name": "fn_add_numbers",
        "parameters": {
            "a": -49654658.45,
            "b": 35453.0
        }
    },
    {
        "prompt": "What is the product of 1, 2 and 3?",
        "name": "fn_get_square_root",
        "parameters": {
            "a": 1.0
        }
    }
]
-> Training with old functions too much makes their logits higher than usual so they are more likely to be selected.
Will test without them to see if we can "restore the logits balance". Also improve prompt to ask the llm to think befroe outputing.

- Processing time too long
-> need to reduce the token the llm generates in general
-> directly inject the argument if there is only one available option

x - signs are sometimes lost in free generation when the number is complicated
[
    {
        "prompt": "What is -41878.57 divided by twenty-nine",
        "name": "fn_divide_numbers",
        "parameters": {
            "a": 41878.57,
            "b": 29.0
        }
    }
]
-> It seems that the function is working properly but the llm sometimes does not produce the minus sign. 

x When there is a newline in quoted string, only get the part before the newline because generation stops at \n
-> The problem is regex does not extract beyond \n. So quoted string will not be extracted correctly if they contain a newline.
-> use re.DOTALL to match a newline too

x for constrained regex generation, do not allow mid_sentence version (no leading space)
-> add a flag in tokenize_str() for including mid sentence version or not.